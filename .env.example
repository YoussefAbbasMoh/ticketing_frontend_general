# Optional override for API (defaults match src/services/api.js)
VITE_API_BASE_URL=https://ticketing-backend-general.vercel.app/api

# Only needed for SSR/tests; in the browser registration uses window.location.origin
VITE_MAIN_APP_URL=http://localhost:3000
